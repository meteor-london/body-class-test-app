# Test app for london:body-class

Symlink https://github.com/meteor-london/body-class into the packages dir.

run it, click the counter, see the background get darker as the dynamic body class changes.
